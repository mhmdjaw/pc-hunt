import { makeStyles } from "@material-ui/core/styles";

const useAuthLayoutStyles = makeStyles({
  headline: {
    margin: "6vh 0 2vh 0",
    fontWeight: 700,
  },
  paper: {
    padding: "10%",
    marginBottom: "5vh",
  },
});

export default useAuthLayoutStyles;
