import { makeStyles } from "@material-ui/core";

const useOrdersStyles = makeStyles({
  title: {
    marginBottom: "32px",
    fontWeight: 500,
  },
  searchInput: {
    marginBottom: "24px",
  },
});

export default useOrdersStyles;
