import { makeStyles } from "@material-ui/core";

const useWishlistStyles = makeStyles({
  title: {
    marginBottom: "32px",
    fontWeight: 500,
  },
  loading: {
    height: "120px",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    paddingBottom: "30px",
  },
});

export default useWishlistStyles;
